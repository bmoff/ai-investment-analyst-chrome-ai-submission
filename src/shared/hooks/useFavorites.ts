/**
 * Re-export useFavorites from FavoritesContext for backward compatibility
 * This allows existing imports to continue working
 */
export { useFavorites, type Favorite } from '@/shared/contexts/FavoritesContext';
