# Environment Variables
# Copy this file to .env.local and fill in your values
# Next.js automatically loads .env.local files (they are gitignored)

# ============================================
# Chrome AI Origin Trial Token
# ============================================
# For localhost testing: Leave empty or use the default token included in the app
# For production: Register at https://developer.chrome.com/docs/ai/rewriter-api
#   and add your registered token here
NEXT_PUBLIC_CHROME_AI_TOKEN=

# ============================================
# Stock Data API (Optional - app works with mock data if not provided)
# ============================================
# Get a free API key from: https://finnhub.io/register
# Free tier: 60 calls/minute - perfect for testing and hackathons!
# Leave empty to use mock data (supports: AAPL, TSLA, MSFT, GOOGL, AMZN, NVDA, META)
FINNHUB_API_KEY=

# ============================================
# Notes
# ============================================
# - Chrome AI APIs work on localhost without a token (just need flags enabled)
# - The app includes a default localhost token, so NEXT_PUBLIC_CHROME_AI_TOKEN is optional for local testing
# - FINNHUB_API_KEY is optional - the app includes mock data for popular stocks (AAPL, TSLA, MSFT, GOOGL, AMZN, NVDA, META)
#   Configure a real API key for access to all stocks and real-time data